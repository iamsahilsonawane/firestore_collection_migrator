### Simple firestore collection duplicator between diff Firebase projects

#### To use
1. Add add source and destination project's service-accounts (json) file to `./service_accounts` directory as `from.json` & `to.json`. 
2. Run python3 migrate.py collection_name
